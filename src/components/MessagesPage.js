import React from 'react';

const MessagesPage = () => {
  return (
    <div className="flex flex-col items-center justify-center text-center p-8 bg-surface rounded-2xl shadow-lg animate-fade-in">
      <h2 className="text-2xl font-bold text-on-surface mb-2">Mensajes</h2>
      <p className="text-on-surface-variant">Esta sección está en construcción.</p>
      <p className="text-on-surface-variant mt-4 max-w-md">
        En esta sección podrás comunicarte directamente con otros usuarios para coordinar el alquiler, hacer preguntas sobre los trajes y gestionar tus reservas.
      </p>
    </div>
  );
};

export default MessagesPage;
